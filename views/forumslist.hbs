<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
<h1>{{ title }}</h1>
{{#if forums.length}}
    <ul>
        {{#each forums}}
            <li><a href="/forums/{{ this.id }}">{{ this.title }}</a></li>
        {{/each}}
    </ul>
{{else}}
    <p>No open forums available.</p>
{{/if}}
</body>
</html>
